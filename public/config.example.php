<?php
/**
 * Per-server configuration. Copy this file to `config.php` (which is
 * gitignored) and set the full path to a Python 3 interpreter if the
 * auto-detection in lib.php does not find one on your host.
 *
 *   cp config.example.php config.php
 *
 * Leave it unset to let lib.php look for `python3` on PATH and in the common
 * locations.
 */

// define('PYTHON_BIN', '/usr/bin/python3');
